interface IAvatar{
    id           ? :number;
    name         ? :string;
    exp          ? :number;
    silver       ? :number;
    gold         ? :number;
    redPowder    ? :number;
    diamond      ? :number;
    createdDate  ? :string;
}